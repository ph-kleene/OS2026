1. 在linux环境中采用nasm汇编语言实现write、sleep和exit三个库函数，封装write, select和exit三个系统调用，在mylib.h中声明三个库函数，并利用上述三个库函数实现一个每隔1秒钟打印"hello world"（共计3次）的C语言程序。(需要分别实现32位版本和64位版本)
2. 对于程序员⽽⾔，系统调⽤是通过库函数调⽤来完成的。有必要让程序员了解哪个库函数调⽤会导致了系统调⽤吗？如果有必要，请说明原因。
3. 画出lab2中app调用sleep库函数引发的系统调用过程时序图，需要刻画出用户态的app和sleep库代码，核心态的OS内核函数代码的执行过程。